import { Box, Checkbox, Text } from "@chakra-ui/react";
import { useTheme } from "@emotion/react";

const TablaBonos = () => {};

const Home = () => {
  const theme = useTheme();

  return (
    <Box>
      <Text>Contenido</Text>
    </Box>
  );
};

export default Home;
